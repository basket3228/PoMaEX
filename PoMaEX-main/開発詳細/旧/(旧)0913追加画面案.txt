パソコン時
・画面を3つに分割。
・常時バディーズ一覧をサイドに表示。
・ない場合は該当のPoMaToolsページをiframeでサイドに表示。
    (画像)あればサイドに表示。(スクロール可能に)
・追加、更新、削除が同じ画面で実現できるように。
    ↑ドラッグアンドドロップで操作が出来るように。
    削除はボタン(確認の後実行。)。

スマホ時(もし実装なら、、)
・サイドに表示はなくなる。
・サイドの表示はクリックでポップアウトで出てくる。


